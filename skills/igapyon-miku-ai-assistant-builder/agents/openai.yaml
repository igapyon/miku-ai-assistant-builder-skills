interface:
  display_name: "Miku AI Assistant Builder"
  short_description: "Agent Builder向け配備資料を二段階で作成"
  default_prompt: "Use $igapyon-miku-ai-assistant-builder to prepare or resume a two-stage folder conversion primarily for Microsoft 365 Copilot Agent Builder. Google Gemini Gem support is early access; when selected, confirm whether Markdown should remain Markdown or be converted to DOCX."
